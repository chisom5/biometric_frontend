import styled from "styled-components";
import { base } from "../../../styles/baseStyle";

const Img = styled.img`
  display: block;
  max-width: 100%;
  height: auto;
  ${base}
`;

export default Img;
